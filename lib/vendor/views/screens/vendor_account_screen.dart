import 'package:flutter/material.dart';

class VendorAccountScreen extends StatelessWidget {
  const VendorAccountScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Account Screen'),
    );
  }
}
