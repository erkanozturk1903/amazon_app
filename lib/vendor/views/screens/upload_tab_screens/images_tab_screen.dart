import 'package:flutter/material.dart';

class ImagesTabScreen extends StatelessWidget {
  const ImagesTabScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('İmages Tab Screen'),
    );
  }
}
