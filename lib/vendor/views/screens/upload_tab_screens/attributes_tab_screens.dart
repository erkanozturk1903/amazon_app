

import 'package:flutter/material.dart';

class AttributesTabScreens extends StatelessWidget {
  const AttributesTabScreens({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Attributes Screen'),
    );
  }
}
